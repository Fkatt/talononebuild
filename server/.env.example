# Server Configuration
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
# Coolify will provide this URL after creating the PostgreSQL resource
DATABASE_URL=postgresql://user:pass@localhost:5432/talonforge

# Security Keys
# Generate these using setup-coolify.sh script
ENCRYPTION_KEY=generate-32-byte-random-string
JWT_SECRET=generate-64-byte-random-string
